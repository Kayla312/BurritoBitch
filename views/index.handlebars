<h1>BurritoBitch!</h1>

<h2>Not Yet Consumed</h2>

<ul>
  {{#each burritos}}
    {{#unless consumed}}
      {{> burritos/ consumed=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burritos that are consumed!</h2>

<ul>
  {{#each burritos}}
    {{#if consumed}}
      {{> burritos/ consumed=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burrito</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burrito Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="consumed">consumed?</label><br>
    <input type="radio" name="consumed" value="1" checked> consumed!<br>
    <input type="radio" name="consumed" value="0"> Not Consumed
  </div>

  <button type="submit">Add Burrito, Bitch!</button>
</form>
